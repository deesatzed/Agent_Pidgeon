# Contract Fixtures

These fixtures are small, deterministic inputs for the `agent-pidgin` CLI.

They are intentionally local and JSON-first. They do not require Hugging Face authentication or A2A/MCP SDKs.

